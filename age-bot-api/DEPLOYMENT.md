# 🚀 Age-bot API - Инструкция по развертыванию на Timeweb

## ✅ Что создано

Flask API сервис для определения возраста по фотографии:
- **app.py** - Flask приложение с endpoints
- **requirements.txt** - Python зависимости
- **models/** - MXNet модели (m1 - MobileNet 0.25X)
- **deploy.sh** - Скрипт автоматического развертывания

## 📋 Пошаговая инструкция

### Вариант 1: Автоматическое развертывание (рекомендуется)

```bash
cd "/Users/alexeipinaev/Documents/Rejuvena/age-bot-api"
./deploy.sh
```

Скрипт автоматически:
1. Создаст директорию на сервере
2. Загрузит все файлы и модели
3. Установит зависимости
4. Запустит API на порту 5000

### Вариант 2: Ручное развертывание

#### Шаг 1: Подключение к серверу
```bash
ssh root@37.252.20.170
```
Пароль: `c+d2Ei@GeWWKq8`

#### Шаг 2: Подготовка сервера
```bash
# Установка Python и зависимостей
apt update
apt install -y python3 python3-pip python3-venv

# Создание директории
mkdir -p /var/www/age-bot-api
cd /var/www/age-bot-api
```

#### Шаг 3: Загрузка файлов (с локальной машины)
```bash
# В новом терминале на локальной машине
cd "/Users/alexeipinaev/Documents/Rejuvena/age-bot-api"
scp -r * root@37.252.20.170:/var/www/age-bot-api/
```

#### Шаг 4: Установка зависимостей (на сервере)
```bash
cd /var/www/age-bot-api
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

#### Шаг 5: Запуск сервиса
```bash
# Для тестирования
python app.py

# Для production (в фоне)
nohup gunicorn --bind 0.0.0.0:5000 --workers 2 --timeout 120 app:app > logs.txt 2>&1 &
```

## 🧪 Тестирование API

### Проверка здоровья
```bash
curl http://37.252.20.170:5000/health
```

Ответ:
```json
{
  "status": "ok",
  "model_loaded": true
}
```

### Тест определения возраста
```bash
curl -X POST http://37.252.20.170:5000/api/estimate-age \
  -H "Content-Type: application/json" \
  -d '{
    "image": "data:image/jpeg;base64,/9j/4AAQ..."
  }'
```

## 🔧 Управление сервисом

### Остановка
```bash
ssh root@37.252.20.170
pkill -f "gunicorn.*age-bot-api"
```

### Перезапуск
```bash
ssh root@37.252.20.170
cd /var/www/age-bot-api
source venv/bin/activate
pkill -f "gunicorn.*age-bot-api"
nohup gunicorn --bind 0.0.0.0:5000 --workers 2 --timeout 120 app:app > logs.txt 2>&1 &
```

### Просмотр логов
```bash
ssh root@37.252.20.170
tail -f /var/www/age-bot-api/logs.txt
```

## 🌐 Интеграция с фронтендом

API будет доступен по адресу:
```
http://37.252.20.170:5000
```

Endpoints:
- `GET /health` - проверка работоспособности
- `POST /api/estimate-age` - определение возраста

## 🔒 Рекомендации по безопасности

1. **Настроить Nginx как reverse proxy**
2. **Добавить SSL сертификат**
3. **Настроить firewall**
4. **Добавить rate limiting**
5. **Настроить API key authentication**

## ❓ Troubleshooting

### Ошибка: "Model not loaded"
Проверьте что файлы моделей скопированы:
```bash
ls -la /var/www/age-bot-api/models/
```

### Ошибка: "Port already in use"
Остановите старый процесс:
```bash
pkill -f "gunicorn.*age-bot-api"
```

### Ошибка при установке mxnet
Попробуйте установить конкретную версию:
```bash
pip install mxnet==1.9.1
```

## 📊 Следующие шаги

После успешного развертывания:
1. Протестируйте API с реальными фотографиями
2. Обновите фронтенд для использования реального API
3. Настройте мониторинг и логирование
4. Добавьте обработку ошибок
