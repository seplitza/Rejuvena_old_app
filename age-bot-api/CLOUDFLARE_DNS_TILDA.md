# Настройка DNS в Cloudflare для Tilda + API

## Проблема
После переключения на Cloudflare nameservers основной сайт seplitza.ru перестал работать, потому что DNS записи для Tilda не были добавлены в Cloudflare.

## Решение
Добавить DNS записи в Cloudflare для работы как Tilda сайта, так и API сервера.

---

## Шаг 1: Войдите в Cloudflare Dashboard

1. Перейдите на https://dash.cloudflare.com
2. Войдите в свой аккаунт
3. Выберите домен **seplitza.ru**

---

## Шаг 2: Откройте раздел DNS

В левом меню выберите **DNS** → **Records**

---

## Шаг 3: Добавьте записи для Tilda

### Запись 1: Основной домен (A запись)

Нажмите **Add record** и заполните:

- **Type**: `A`
- **Name**: `@` (это означает seplitza.ru)
- **IPv4 address**: `185.230.60.1`
- **Proxy status**: **Proxied** (включите оранжевое облако 🟠)
- **TTL**: `Auto`

Нажмите **Save**

### Запись 2: WWW поддомен (CNAME)

Нажмите **Add record** и заполните:

- **Type**: `CNAME`
- **Name**: `www`
- **Target**: `domains.tildacdn.com`
- **Proxy status**: **Proxied** (включите оранжевое облако 🟠)
- **TTL**: `Auto`

Нажмите **Save**

---

## Шаг 4: Проверьте запись для API (должна уже быть)

В списке DNS записей должна быть:

- **Type**: `A`
- **Name**: `api`
- **IPv4 address**: `37.252.20.170`
- **Proxy status**: **Proxied** (🟠)

Если её нет - добавьте по аналогии с шагом 3.

---

## Шаг 5: Проверка

После добавления записей у вас должно быть **3 DNS записи**:

| Type  | Name | Target/IP          | Proxy  |
|-------|------|--------------------|--------|
| A     | @    | 185.230.60.1       | 🟠 Да  |
| CNAME | www  | domains.tildacdn.com| 🟠 Да  |
| A     | api  | 37.252.20.170      | 🟠 Да  |

---

## Шаг 6: Ожидание (5-30 минут)

DNS изменения распространяются в течение 5-30 минут.

После этого проверьте:

### В браузере:
- ✅ https://seplitza.ru - должен открыться сайт Tilda
- ✅ https://www.seplitza.ru - должен открыться сайт Tilda
- ✅ https://api.seplitza.ru - должен показать "Age-bot API is running"

### В терминале на сервере:
```bash
# Проверка DNS
dig seplitza.ru A +short
# Должно показать IP Cloudflare (например, 104.21.x.x или 172.67.x.x)

dig api.seplitza.ru A +short
# Тоже должно показать IP Cloudflare
```

---

## Важные моменты

### 1. Proxy Status (оранжевое облако 🟠)
- **Включено** - трафик идёт через Cloudflare (SSL, защита, кеширование)
- **Выключено** - прямое соединение к серверу

Для всех трёх записей **включайте Proxied**!

### 2. IP адрес Tilda
Мы используем `185.230.60.1` - это основной IP Tilda.

Если у вас другой IP (можно узнать в настройках проекта Tilda), используйте его.

### 3. Nameservers на dnar.ru
Убедитесь что на dnar.ru в настройках домена seplitza.ru указаны nameservers Cloudflare (не ns1.tildadns.com):

```
aisha.ns.cloudflare.com
doug.ns.cloudflare.com
```

(или другие, которые показал вам Cloudflare)

---

## Альтернативное решение

Если вы хотите вернуть всё как было:

### На dnar.ru смените nameservers обратно на:
```
ns1.tildadns.com
ns2.tildadns.com
```

**НО тогда:**
- ❌ api.seplitza.ru не будет работать через Cloudflare (без SSL)
- ❌ Придётся настраивать SSL на сервере вручную

**Рекомендую:** использовать Cloudflare для всех поддоменов (вариант из этой инструкции).

---

## Проверка статуса

### Сразу после добавления записей:
```bash
# С вашего компьютера
curl -I https://seplitza.ru
# Должен вернуть код 200 и заголовок "server: cloudflare"

curl -I https://api.seplitza.ru
# Должен вернуть код 200 и "Age-bot API is running"
```

### Если не работает через 30 минут:
1. Проверьте что nameservers на dnar.ru указывают на Cloudflare
2. Проверьте что в Cloudflare все 3 записи добавлены и Proxy включен (🟠)
3. Очистите DNS кеш на компьютере:
   - **macOS**: `sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder`
   - **Windows**: `ipconfig /flushdns`
   - **Linux**: `sudo systemd-resolve --flush-caches`

---

## Итог

После настройки у вас будет:

✅ **seplitza.ru** → Tilda сайт через Cloudflare SSL  
✅ **www.seplitza.ru** → Tilda сайт через Cloudflare SSL  
✅ **api.seplitza.ru** → Age-bot API через Cloudflare SSL  

Все работает с HTTPS, все защищено Cloudflare! 🎉
